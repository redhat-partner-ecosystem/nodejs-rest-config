apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodejs-build-workspace
spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
